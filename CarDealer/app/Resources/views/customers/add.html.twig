{% extends 'base.html.twig' %}

{% block body %}
    {{ form_errors(form.name) }}
    {{ form_errors(form.birthdate) }}
    {{ form_start(form) }}
    {#{{ form_row(form.name) }}#}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h2 style="color: #67b168">Customer form</h2>
                <div class="form-group">
                    <div>{{ form_row(form.name) }}</div>
                </div>
                <div class="form-group">
                    <div>{{ form_row(form.birthdate) }}</div>
                </div>
                <div class="form-group">
                    <div>{{ form_row(form.Save) }}</div>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}

